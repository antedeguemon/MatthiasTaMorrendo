<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/statusFragment">

    <fragment
        android:id="@+id/statusFragment"
        android:name="pl.vicente.matthiastamorrendo.StatusFragment"
        android:label="Matthias tá morrendo?"
        tools:layout="@layout/fragment_status" />
    <fragment
        android:id="@+id/aboutFragment"
        android:name="pl.vicente.matthiastamorrendo.AboutFragment"
        android:label="Sobre o Matthias"
        tools:layout="@layout/fragment_about" />
</navigation>